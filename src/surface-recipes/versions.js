export const surfaceModelVersion = "paper-surface-js-r1";
export const surfaceRecipeSchemaVersion = 1;

export const SURFACE_VERSIONS = Object.freeze({
  surfaceModelVersion,
  surfaceRecipeSchemaVersion,
});
